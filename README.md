ftp-git is GUI ftp client which can upload files tracked by git to ftp server. 
It's based on PySide, so it's cross-platform (supporting Windows, Linux, macOS, etc.)

[Ftp-Git](http://redino.net/ftp-git)